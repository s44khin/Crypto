buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath(Dependencies.Plugins.GRADLE)
        classpath(Dependencies.Plugins.KOTLIN)
    }
}